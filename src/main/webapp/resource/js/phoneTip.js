function frozen_warn(content){
	$.tips({
        content:content,
        stayTime:2000,
        type:"warn"
    });
}
function frozen_info(content){
	$.tips({
        content:content,
        stayTime:2000,
        type:"info"
    });
}
